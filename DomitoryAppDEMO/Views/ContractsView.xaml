<UserControl x:Class="DomitoryAppDEMO.Views.ContractsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="24">
            <!-- Заголовок -->
            <StackPanel Margin="0 0 0 24">
                <TextBlock Text="Управление договорами" 
                           FontSize="28" 
                           FontWeight="SemiBold"
                           Foreground="#1F2937"/>
                <TextBlock Text="Создание, редактирование и отслеживание договоров на проживание" 
                           Foreground="#6B7280" 
                           FontSize="16"
                           Margin="0 4 0 0"/>
            </StackPanel>

            <!-- Панель инструментов -->
            <Border Background="White"
                    CornerRadius="12"
                    BorderThickness="1"
                    BorderBrush="#E5E7EB"
                    Padding="20"
                    Margin="0 0 0 24">
                <StackPanel>
                    <TextBlock Text="Быстрые действия" 
                               FontSize="18" 
                               FontWeight="SemiBold"
                               Margin="0 0 0 16"/>

                    <WrapPanel>
                        <Button Background="#3B82F6"
                                Foreground="White"
                                FontWeight="SemiBold"
                                BorderThickness="0"
                                Padding="16 12"
                                Margin="0 0 12 12"
                                Cursor="Hand"
                                >
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border" 
                                            Background="{TemplateBinding Background}"
                                            CornerRadius="8">
                                        <ContentPresenter HorizontalAlignment="Center" 
                                                          VerticalAlignment="Center" />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📝" 
                                           FontSize="16"
                                           Margin="0 0 8 0"/>
                                <TextBlock Text="Новый договор" Width="99"/>
                            </StackPanel>
                        </Button>

                        <Button Background="White"
                                Foreground="#374151"
                                BorderBrush="#D1D5DB"
                                BorderThickness="1"
                                Padding="16 12"
                                Margin="0 0 12 12"
                                Cursor="Hand"
                                >
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border" 
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="8">
                                        <ContentPresenter HorizontalAlignment="Center" 
                                                          VerticalAlignment="Center" />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#F9FAFB"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🔍" 
                                           FontSize="16"
                                           Margin="0 0 8 0"/>
                                <TextBlock Text="Поиск договоров" Width="110"/>
                            </StackPanel>
                        </Button>

                        <Button Background="White"
                                Foreground="#374151"
                                BorderBrush="#D1D5DB"
                                BorderThickness="1"
                                Padding="16 12"
                                Margin="0 0 12 12"
                                Cursor="Hand" Width="141"
                                >
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border" 
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="8">
                                        <ContentPresenter HorizontalAlignment="Center" 
                                                          VerticalAlignment="Center" />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#F9FAFB"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🖨️" 
                                           FontSize="16"
                                           Margin="0 0 8 0"/>
                                <TextBlock Text="Печать договоров"/>
                            </StackPanel>
                        </Button>
                    </WrapPanel>
                </StackPanel>
            </Border>

            <!-- Информация -->
            <Border Background="White"
                    CornerRadius="12"
                    BorderThickness="1"
                    BorderBrush="#E5E7EB"
                    Padding="24">
                <StackPanel>
                    <TextBlock Text="Статистика договоров" 
                               FontSize="18" 
                               FontWeight="SemiBold"
                               Margin="0 0 0 16"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Активные договоры -->
                        <Border Grid.Column="0" 
                                Background="#F0F9FF" 
                                CornerRadius="8"
                                Padding="16"
                                Margin="0 0 12 0">
                            <StackPanel>
                                <TextBlock Text="156" 
                                           FontSize="24" 
                                           FontWeight="SemiBold"
                                           Foreground="#0EA5E9"/>
                                <TextBlock Text="Активных договоров" 
                                           Foreground="#6B7280"/>
                            </StackPanel>
                        </Border>

                        <!-- Истекают скоро -->
                        <Border Grid.Column="1" 
                                Background="#FEF3C7" 
                                CornerRadius="8"
                                Padding="16"
                                Margin="6 0">
                            <StackPanel>
                                <TextBlock Text="23" 
                                           FontSize="24" 
                                           FontWeight="SemiBold"
                                           Foreground="#D97706"/>
                                <TextBlock Text="Истекают в этом месяце" 
                                           Foreground="#6B7280"/>
                            </StackPanel>
                        </Border>

                        <!-- Завершённые -->
                        <Border Grid.Column="2" 
                                Background="#F3F4F6" 
                                CornerRadius="8"
                                Padding="16"
                                Margin="12 0 0 0">
                            <StackPanel>
                                <TextBlock Text="89" 
                                           FontSize="24" 
                                           FontWeight="SemiBold"
                                           Foreground="#6B7280"/>
                                <TextBlock Text="Завершённых" 
                                           Foreground="#6B7280"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>